import Typography from '@/components/ui/Typography';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function SkillsList() {
  return (
    <SafeAreaView>
      <Typography size="xl">Skill List</Typography>
    </SafeAreaView>
  );
}
