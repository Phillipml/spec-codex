import { Text, View } from 'react-native';

export default function HomeScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-bgPrimary">
      <Text className="text-xl font-bold text-gold">Olá</Text>
    </View>
  );
}
